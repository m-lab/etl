apiVersion: v1
kind: Service
metadata:
  name: etl-parser-status
  namespace: default
spec:
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    run: etl-parser
  sessionAffinity: None
  type: LoadBalancer
